<Page x:Class="spec.Pages.Admin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:spec.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Admin">

    <Grid>
        <Grid Grid.Row="1" Background="#eff542">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,0,0">
                <Border Height="132" Width="152" Margin="0,0,16,0" Background="White">
                    <Image Name="admin123" Height="150" Width="150" Source="/res/admin.jpg"/>
                    <!-- аватарка-->
                </Border>

                <TextBlock Name="UserTB_2" Text="Иванов И И" VerticalAlignment="Center" Margin="0,0,16,0" Foreground="Black" FontSize="16"/>
                <!--имя пользователя -->
                <TextBlock Name="RoleTB_2" Text="(Администратор)" VerticalAlignment="Center" Foreground="Black" FontSize="14"/>
                <!-- роль пользователя-->
            </StackPanel>
        </Grid>
    </Grid>
</Page>
